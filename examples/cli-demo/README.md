# cli-demo

Runs the real CLI in a fresh temp directory: `demo`, then `audit:verify`.

```bash
node examples/cli-demo/main.js
```

Expect the zero-config demo (allow, block, mask) followed by a clean hash-chain verification.
