# node-basic

Minimal Node.js SDK example: wrap a tool, watch it get blocked.

```bash
node examples/node-basic/main.js
```

Expect an allowed call, then a blocked one. The audit trail is written to `.grimdall/audit.json` in your current directory.
